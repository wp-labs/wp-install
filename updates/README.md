# updates manifest metadata

This repository stores update metadata for Warp Parse.

- Primary manifests: `updates/<channel>/manifest.json`
- Optional history: `updates/<channel>/versions/*.json`

Channels:
- stable
- beta
- alpha

All fields should be generated by release CI after asset publishing.
